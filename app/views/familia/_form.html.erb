  <div id="box">
	<%= form_for(@familium) do |f| %>
		<% if @familium.errors.any? %>
			<div id="error_explanation">
			<h2><%= pluralize(@familium.errors.count, "erro") %> esta familia não pode ser salvo:</h2>

			<ul>
				<% @familium.errors.full_messages.each do |msg| %>
				<ol><%= msg %></ol>
				<% end %>
			</ul>
			</div>
	<% end %>  
	    		<form id="CadastrarClasse">
            	<!-- tabela de preencher -->
    			<table class="table_geral">
                    <tr>
                        <td>Ordem:</td>
                        <td>
                        	<%= f.select :ordem_id, Ordem.find(:all).collect { |item| [item.descricao,item.id] } %>
                        </td>
                        <td style="width: 800px;"></td>
                    </tr>
                    <tr>
            			<td>Familia:</td>
            			<td><%= f.text_field :descricao %></td>
            			<td style="width: 800px;"></td>
        			</tr>
    			</table>
    			<br />
                <!-- tabela de botões -->
    			<table border="0" class="table_geral">
        			<tr>
            			<td><%= f.submit "Salvar" %></td>
            			<td><input id="btnCancelar" type="button" value="Cancelar" onclick="javascript:window.location='/familia'"/></td>
            			<td style="width: 800px;"></td>
        			</tr>
    			</table>
    		</form>
<% end %>