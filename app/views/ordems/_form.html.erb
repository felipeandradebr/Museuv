  <div id="box">
	<%= form_for(@ordem) do |f| %>
		<% if @ordem.errors.any? %>
			<div id="error_explanation">
			<h2><%= pluralize(@ordem.errors.count, "erro") %> esta ordem não pode ser salvo:</h2>

			<ul>
				<% @ordem.errors.full_messages.each do |msg| %>
				<ol><%= msg %></ol>
				<% end %>
			</ul>
			</div>
	<% end %>  
	    		<form id="CadastrarClasse">
            	<!-- tabela de preencher -->
    			<table class="table_geral">
                    <tr>
                        <td>Clase:</td>
                        <td>
                        	<%= f.select :clase_id, Clase.find(:all).collect { |item| [item.descricao,item.id] } %>
                        </td>
                        <td style="width: 800px;"></td>
                    </tr>
                    <tr>
            			<td>Ordem:</td>
            			<td><%= f.text_field :descricao %></td>
            			<td style="width: 800px;"></td>
        			</tr>
    			</table>
    			<br />
                <!-- tabela de botões -->
    			<table border="0" class="table_geral">
        			<tr>
            			<td><%= f.submit "Cadastrar" %></td>
            			<td><input id="btnCancelar" type="button" value="Cancelar" onclick="javascript:window.location='/ordems'"/></td>
            			<td style="width: 800px;"></td>
        			</tr>
    			</table>
    		</form>
<% end %>