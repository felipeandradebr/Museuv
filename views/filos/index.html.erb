<h1>Filos</h1>

<table>
  <tr>
    <th>Descrição</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @filos.each do |filo| %>
  <tr>
    <td><%= filo.descricao %></td>
    <td><%= filo.status %></td>
    <td><%= link_to 'Visualizar', filo %></td>
    <td><%= link_to 'Editar', edit_filo_path(filo) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Filo', new_filo_path %>
